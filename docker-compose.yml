version: '3'
services:
  db:
    image: postgres
  web:
    build: .
    volumes:
      - .:/aws_demo_app
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      PGHOST: "db"
      PGUSER: "postgres"
      PGDBNAME: "aws_demo_app"
    depends_on:
      - db
